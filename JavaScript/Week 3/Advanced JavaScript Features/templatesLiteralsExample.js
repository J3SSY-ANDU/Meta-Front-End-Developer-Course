// `${}`syntax

// concatanation syntax
let greet = "Hello";
let place = "World";
console.log(`${greet} ${place} !`) //display both variables using template literals
// multiple line
console.log(`Hello,
World
!
`);